# Gemini API key for board image analysis (optional).
# Set in Vercel: Project Settings → Environment Variables → GEMINI_API_KEY
# For local dev with API: run `vercel dev` so /api/analyze-board is available.
GEMINI_API_KEY=
